I want you to help me design and build the .net library that will support us in generating the topology.json file that is used by the dashboard. We can keep the library code in this repo until we are done when I will move it to its own repo.
Complexity is our enememy. We must keep things simple and you must alert me if some concept require a lot of complexity in which we might pause the implementation of that feature til we understand it better. 

The library will consist of 2 things, the models used to build the json file and helpers to help the library user define the model. The main model is SystemTopology, which has a SystemName. It has a list of Components. A component have a type property which matches our blocks. Start with Extractor, Loader, and TransactionalIntegration. There is a list of resources which also has types. We start with Type Topic now but we will later implement Database. We also have Connectors and APIs, and Services (which are dependencies to existing shared services) but I think we will wait with the implementation of those