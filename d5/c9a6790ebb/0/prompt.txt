The docs seems to be stale now: "One thing outside this task worth flagging: docs/reference/cli/create.md is badly out of date — it describes intropy int create as writing a single Blueprint YAML file (blueprints/hello.yaml), which bears no resemblance to the ASP.NET project the CLI now scaffolds. It also documents the old <name> positional rather than the blueprint-selector + -n model. Want me to rewrite that reference page to match the current CLI?"